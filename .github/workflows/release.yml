name: Build and Release

on:
  release:
    types: [published]

env:
  MOSQ_VERSION: "2.1.2"

jobs:
  build:
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            arch: x86_64
          - runner: ubuntu-24.04-arm
            arch: arm64

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            perl \
            bison \
            flex \
            libcjson-dev \
            zlib1g-dev

      - name: Install Mosquitto headers
        run: |
          wget -qO /tmp/mosquitto.tar.gz \
            "https://github.com/eclipse-mosquitto/mosquitto/archive/refs/tags/v${MOSQ_VERSION}.tar.gz"
          tar -xzf /tmp/mosquitto.tar.gz -C /tmp
          sudo cp -r /tmp/mosquitto-${MOSQ_VERSION}/include/. /usr/local/include/

      - name: Cache PostgreSQL build
        uses: actions/cache@v4
        with:
          path: |
            build/libpq_ext-prefix
            build/libpq-install
          key: libpq-17.2-${{ matrix.arch }}

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel

      - name: Upload to release
        run: |
          cp build/mosquitto_pgsql_auth.so mosquitto_pgsql_auth-${{ matrix.arch }}.so
          gh release upload "${{ github.event.release.tag_name }}" \
            "mosquitto_pgsql_auth-${{ matrix.arch }}.so"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
